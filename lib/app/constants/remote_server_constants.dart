class RemoteServerConstants {
  static const String baseUrl = "https://reqres.in";

  static const String remoteServerLogin = "$baseUrl/api/login";
  static const String remoteServerUserList = "$baseUrl/api/users";
}
